# 自托管服务器配置。复制为 server/.env 后填写。
# API Key 不填则自动读取 web/.env 的 VITE_ANTHROPIC_API_KEY。
#ANTHROPIC_API_KEY=sk-ant-api03-xxxx

# 帐密列表(逗号分隔,user:pass),发给朋友用
AUTH_USERS=admin:changeme

# 端口(默认 8787)
#PORT=8787

# 会话签名密钥(不填则每次启动随机=重启后需重新登录)
#SESSION_SECRET=any-long-random-string
